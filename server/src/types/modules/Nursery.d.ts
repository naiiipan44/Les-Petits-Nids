export interface Nursery {
  id: number;
  name: string;
  address: string;
  zipCode: string;
  phoneNumber: string;
  capacity: number;
  availability: string;
  gestion: boolean;
  minAge: number;
  maxAge: number;
  isDisabled: boolean;
}
